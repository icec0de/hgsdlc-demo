# Supercompany

Static client-facing web app: index.html, style.css, app.js. No build step.

Started as a plain "Acme Coffee" page (see `specs/changes/T-0000/master.md` for
that original baseline) and reached this state through tasks T-0001 to T-0006,
each recorded under `specs/changes/`.
